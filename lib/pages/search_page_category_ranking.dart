part of 'search_page.dart';

class CategoryRankingWidget extends StatelessWidget {
  const CategoryRankingWidget({super.key, required this.pageBlock});
  final CategoryRankingPageBlock pageBlock;
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      color: Colors.blue,
    );
  }
}
